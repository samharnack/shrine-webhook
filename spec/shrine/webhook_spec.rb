require 'spec_helper'

describe Shrine::Webhook do
  it 'has a version number' do
    expect(Shrine::Webhook::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
